#服务器启动配置，端口号
server:
  port: ${port:9090}

#数据库的相关配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/address_book?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: HY5201314

#mybatis配置(!!!)
mybatis:
  mapper-locations: classpath:mapper/*.xml #扫描mapper的所有.xml结尾的文件
  configuration:
    map-underscore-to-camel-case: true